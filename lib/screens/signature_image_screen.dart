import 'package:flutter/material.dart';

class SignatureImageScreen extends StatelessWidget {
  const SignatureImageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
